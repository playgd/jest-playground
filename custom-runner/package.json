{
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "yarn test -- --watch",
    "lint": "standard --verbose"
  },
  "standard": {
    "globals": [
      "it",
      "expect"
    ]
  },
  "dependencies": {
    "jest": "^21.2.1",
    "standard": "^10.0.3"
  }
}
