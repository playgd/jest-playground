{
  "scripts": {
    "test": "jest --coverage",
    "lint": "echo 'Running lint...' && standard --verbose | snazzy && 'Lint ok!\n'"
  },
  "dependencies": {
    "jest": "^21.2.1",
    "snazzy": "^7.0.0",
    "standard": "^10.0.3"
  }
}
