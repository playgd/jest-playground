{
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "yarn test -- --watch",
    "lint": "echo 'Running lint...' && standard --verbose | snazzy && echo 'Lint ok!'"
  },
  "standard": {
    "globals": [
      "it",
      "expect"
    ]
  },
  "devDependencies": {
    "jest": "^21.2.1",
    "snazzy": "^7.0.0",
    "standard": "^10.0.3"
  }
}
